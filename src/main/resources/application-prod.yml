server:
  port: 8080

spring:

  application:
    name: users-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: bts1234bts
    url: jdbc:mysql://ec2-43-200-189-164.ap-northeast-2.compute.amazonaws.com/users?serverTimezone=Asia/Seoul

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://43.200.189.164:8761/eureka